import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Modern E-Commerce | Premium Products",
  description:
    "Browse our curated collection of premium products with advanced filtering and seamless browsing experience",
  generator: "v0.app",
  openGraph: {
    title: "Modern E-Commerce",
    description: "Browse our curated collection of premium products",
    type: "website",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{
            __html: JSON.stringify({
              "@context": "https://schema.org",
              "@type": "WebSite",
              name: "Modern E-Commerce",
              description: "Premium product catalog with advanced filtering",
              url: typeof window !== "undefined" ? window.location.origin : "",
            }),
          }}
        />
      </head>
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
